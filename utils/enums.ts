export enum UserRole {
  "USER",
  "PROVIDER",
  "ADMIN",
}
export enum BookingStatus {
  PENDING,
  CONFIRMED,
  CANCELLED,
}

export enum PaymentStatus {
  PENDING,
  SUCCESS,
  FAILED,
  REFUNDED,
}
